import { button } from './button'

const defaultTheme = {
  palette: {
    primary: "#003F90",
    secondary: "#27A893",
    inverse: "#FFFFFF",
    error: "#ff4646"
  },
  component: {
    button,
  }
}

export {defaultTheme}